{
  "name": "patterns",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && npx tsc",
    "start": "yarn build && node dist/factory.js"
  },
  "devDependencies": {
    "eslint": "^8.53.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.1",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  }
}
